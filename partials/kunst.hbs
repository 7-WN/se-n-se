<section id="kunst" class="domein">
    <h2 class="titel">
	Kunst
    </h2>
    {{#each tags}}
	{{#is tag "kunst"}}
		{{#each pages}}
			<h3>{{data.title}}</h3>
			<p>{{data.summary}}</p>
			<p><a href="{{data.url}}">Meer over {{data.title}}</a></p>
		{{/each}}
	{{/is}}
    {{/each}}
</section>