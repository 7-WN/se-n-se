<section id="gezondheid-en-maatschappij" class="domein">
    <h2 class="titel">
	Gezondheid en Maatschappij
    </h2>
    {{#each tags}}
	{{#is tag "gezondheid-en-maatschappij"}}
		{{#each pages}}
			<h3>{{data.title}}</h3>
			<p>{{data.summary}}</p>
			<p><a href="{{data.url}}">Meer over {{data.title}}</a></p>
		{{/each}}
	{{/is}}
    {{/each}}
</section>