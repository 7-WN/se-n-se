<!doctype html>
<html>
<head>
	<meta charset="utf-8">
    <link href="http://netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../../public/styles/opleidingspagina.css">
	<title>{{title}} - 1-jarige se-n-se opleiding studeren in de provincie Antwerpen</title>
</head>
<body class="{{#page.tags}}{{. }}{{/page.tags}}">  {{! list tags for current page as class name in body}}

	<header>
	<div class="container">
		<h1>{{title}}</h1>
		</div>
	</header>
	<div class="container">
	<main>
		<div class="col-md-12">
			<p class="inleiding">{{summary}}</p>
		</div>
		<div class="col-md-8">
			{{#markdown}}
				{{> body }}
			{{/markdown}}
		</div>
		<div class="col-md-4">
			{{Image}}
		</div>
	</main>
	<section class="scholen">
		{{#each scholen}}
			<article class="school">
			{{include this}}
			</article>
		{{/each}}
	</section>
	</div>
</body>
</html>
